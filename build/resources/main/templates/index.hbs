{{#partial "content"}}

    <h1> List of restaurants</h1>

    {{#if restaurant}}
        {{#each restaurant }}
            <a href="/restaurants/{{id}}">{{name}}</a><br>
            </br>
        {{/each}}
    {{else}}
        <p>No restaurants yet.</p>
    {{/if}}
{{/partial}}

{{> layout.hbs}}